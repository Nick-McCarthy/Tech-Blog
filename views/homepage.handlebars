{{#each blogs as |blog| }}
<div class="card">
  <div class="card-body">
    <a href="/blogs/{{blog.id}}">
      <div>
        <p style="float:right;">Created By: <b>{{user.name}}</b> on {{date_created}}</p>
        <h3>{{blog.title}}</h3>
      </div>
    </a>
  </div>
</div>
{{/each}}